import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'recursive-tasklist',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
